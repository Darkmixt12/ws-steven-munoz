// Codebase `tienda` de Cloud Functions. Todavía sin procesos: cada ticket del
// backend exporta aquí sus callables, triggers y funciones programadas.
// El build hace un bundle con esbuild, así que las librerías del monorepo
// (p. ej. `tienda/domain`) quedan dentro de `lib/index.js`.
export {};
